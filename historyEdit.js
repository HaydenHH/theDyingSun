  // let 机械臂 = document.querySelector("#jixiebi2");
  // let 机械臂3 = document.querySelector("#jixiebi3");

  // 机械臂.style.bottom = -Math.random() * 100 + "px";
  // 机械臂3.style.bottom = -Math.random() * 100 + "px";

  // let 树1 = document.querySelector("#tree1");
  // let 树2 = document.querySelector("#tree2");
  // let 随机位移 = Math.random() * 300 + 50;
  // // 树1.style.transform = `translateX(${随机位移}px)`

  // gsap.set(树1, { x: 随机位移 });

  // 树2.style.bottom = -Math.random() * 100 + "px";